我现在需要在一个 docker 里面运行 openclaw，然后我会把我宿主机的某个目录 /data 挂进去，并且也让这个 docker 去共享宿主机的网络。我现在希望在宿主机中对这个 docker 的权限进行控制，例如：
- 限制它可以访问的 ip 地址和域名
- 限制它可以访问的目录/文件后缀名，并精细到具体的权限
- 等等

我觉得使用 eBPF 做是一个目前比较可行的方案，请帮我评估一下。